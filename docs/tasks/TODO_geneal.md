## Audit the Register/Unregister blocks
- check for correct register/unregister ordering
- check if I need to load classes and create objects for them before loading a panel/operator which uses them (msot likely)