## Audit the Register/Unregister blocks
- [ ] check for correct register/unregister ordering
- [ ] check if I need to load classes and create objects for them before loading a panel/operator which uses them (most likely)

## Brushes Creation
- [ ] add default thumbnails for the brushes
- [ ] add a way to select multiple brushes and delete them
- [ ] add a way to select multiple brushes and move them to a different catalog. catalog management in general needs to be simplified if possible.
- [ ] investigate if there's any way to auto-generate a 3d preview of a brush.
- [ ] brushes don't show in their own category until the user manually toggles that group as active.
- [ ] when both texture paint and sculpt brushes are created there is no way of telling them apart based on name